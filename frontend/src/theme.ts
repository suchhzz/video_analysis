import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2', // синий
    },
    secondary: {
      main: '#dc004e', // красный
    },
  },
})

export default theme
